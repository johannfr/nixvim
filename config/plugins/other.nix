{
  plugins.diffview = {
    enable = true;
  };

  plugins.copilot-lua = {
    enable = true;
    settings = {
      panel = {
        enable = false;
      };

      suggestion = {
        enable = false;
      };
    };
  };
}
