{
  plugins.visual-multi = {
    enable = true;
  };

  plugins.diffview = {
    enable = true;
  };

  plugins.copilot-lua = {
    enable = true;
    settings = {
      panel = {
        enable = false;
      };

      suggestion = {
        enable = false;
      };
    };
  };

  plugins.copilot-chat = {
    enable = true;
  };
}
